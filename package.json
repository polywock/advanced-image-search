{
  "name": "advanced_image_search",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rm -rf build; mkdir -p build/unpacked && cp -a src/ build/unpacked && zip -r build/packed.zip build/unpacked"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/chrome": "0.0.82"
  }
}
